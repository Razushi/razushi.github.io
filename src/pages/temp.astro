---
import Example from '../content/Example.md';
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Updated Layout</title>
    <style>



      /* Reset */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      html, body {
        height: 100%;
        font-family: Arial, sans-serif;
        background-color: #262626; /* Body-home background */
      }

      /* Main Layout */
      .grid-container {
        display: flex;
        flex-direction: column;
        height: 100vh;
      }

      /* Header */
      .header-container {
        display: flex;
        width: 100%;
        height: 72px; /* Header height */
        border-bottom: 1px solid #8a8784; /* Bottom border for header */
      }

      .head-home {
        background-color: #262626; /* Same as body-home */
        flex: 0 0 250px; /* Fixed width */
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        border-right: 1px solid #8a8784; /* Right border */
      }

      .head-content {
        background-color: #181818; /* Same as content-left/middle/right */
        flex: 1; /* Take remaining space */
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        border-bottom: 0px solid #8a8784;
      }

      /* Main Layout */
      main {
        display: flex;
        flex: 1; /* Main content area fills remaining space */
        overflow: hidden; /* Prevent content overflow */
      }

      .body-home {
        /* background-color: #181818; /* Updated background */
        flex: 0 0 250px; /* Fixed width for home column */
        display: flex;
        flex-direction: column; /* Stack top and bottom sections vertically */
        color: white;
        border-right: 1px solid #8a8784; /* Thinner right border */
      }

      .body-home-top {
        background-color: #262626; /* Same as body-home */
        height: 80px; /* Fixed height for top section */
        display: flex;
        align-items: center;
        justify-content: center;
        border-bottom: 1px solid #8a8784; /* Bottom border */
      }

      .body-home-bottom {
        background-color: #262626; /* Same as body-home */
        flex: 1; /* Take up the remaining space */
        display: flex;
        align-items: center;
        justify-content: center;
      }

      /* Shared background for content-left, content-middle, and content-right */
      .content-wrapper {
        display: flex;
        flex: 1;
        position: relative; /* To allow z-index stacking */
        overflow: hidden; /* Prevent overflow issues */
        background: linear-gradient(
          to bottom, 
          rgba(24, 24, 24, 0.1) 0%,  /* Semi-transparent image start */
          rgba(24, 24, 24, 0.3) 50%, /* Gradual fade */
          #262626 100%               /* Solid backdrop color */
        ),
        url('/src/assets/old-god-mordekaiser-greyscale.png'); /* Background image */
        background-size: cover; /* Ensure the image covers the container */
        background-repeat: no-repeat; /* Prevent repeating */
        background-position: top; /* Align the image to the top */
        background-attachment: local; /* Ensures the background scrolls with content */
        min-height: 100%; /* Matches the height of the parent container */
      }

      /* Individual content sections */
      .content-left,
      .content-middle,
      .content-right {
        z-index: 1; /* Place above the shared background */
        background: rgba(24, 24, 24, 0.8); /* Add semi-transparent background for text readability */
      }

      .content-left {
        flex: 0 0 80px; /* Fixed width */
      }

      .content-middle {
        flex: 1; /* Take up remaining space */
        overflow-y: auto; /* Scrollable content */
        padding: 16px;
        padding-top: 80px; /* Added top padding */
      }

      .content-right {
        flex: 0 0 80px; /* Fixed width */
      }

      /* Markdown Content Styling */
      article {
        padding: 16px;
        /* background-color: #181818; /* Updated background */
        border-radius: 8px;
        color: #fbfbf9;
      }
    </style>
  </head>
  <body>
    <div class="grid-container">
      <!-- Header -->
      <div class="header-container">
        <div class="head-home">Home Header</div>
        <div class="head-content">Content Header</div>
      </div>

      <!-- Main Layout -->
      <main>
        

        <div class="body-home">
          <div class="body-home-top">Top Section</div>
          <div class="body-home-bottom">Bottom Section</div>
        </div>        

            <!-- Wrapper with shared background -->
            <div class="content-wrapper">
              <div class="content-left"></div>
              <div class="content-middle">
                <article>
                  <Example />
                </article>
              </div>
              <div class="content-right"></div>
            </div>

      </main>
    </div>
    
  </body>
</html>